<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup policy. Nothing is backed up or transferred: the detections database is a
  GPS trace of surveillance hardware, and the encrypted settings file is bound to this device's
  Keystore and would be unreadable elsewhere. Sessions > Back up… is the user-controlled way to
  move data. (allowBackup="false" in the manifest covers Android 8 - 11 and is belt-and-braces here.)
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
